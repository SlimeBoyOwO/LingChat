---
name: script-demander
description: LingChat 剧本的素材清点技能。统计设计稿要求的素材与剧本实际拥有的素材，算出缺口并推动「补素材或改设计」的收敛。当设计阶段需要确认登场人物与所需背景/音乐/音效/环境音/图片是否齐备时使用。
---

# 素材清点（Demander）

本角色负责「要什么、有什么、差什么」。**清点本身由系统自动完成**（扫描磁盘并与设计稿比对），本技能说明清点口径与缺口处理流程。

## 前置条件

- 设计稿已存在，且每章写了 `素材:` 字段。
- 若你尚未加载 `lingchat-script-editor` 主技能，请先加载它 —— 阶段推进与用户确认门由主技能掌辖。

## 产物

一份素材缺口表：**需求 / 已有 / 缺口 / 结论**。系统会直接给出「需求 − 已有」的差集，你需要在缺口非空时推动收敛。

## 清点口径

剧本的素材按类型分目录存放，引擎按事件类型自动到对应目录查找：

| 类型   | 目录                  | 对应事件字段                |
| ------ | --------------------- | --------------------------- |
| 背景   | `Assets/Backgrounds/` | `background` 的 `imagePath` |
| 音乐   | `Assets/Musics/`      | `music` 的 `musicPath`      |
| 音效   | `Assets/Sounds/`      | `sound` 的 `soundPath`      |
| 环境音 | `Assets/Ambients/`    | `ambient` 的 `ambientPath`  |
| 图片   | `Assets/Pics/`        | `present_pic` 的图片名      |

剧本内只写**素材文件名本身**（如 `夜晚.webp`），不要带 `Backgrounds/`、`Musics/` 这类类型目录前缀。全局素材目录（`game_data/` 下的同名目录）也算已有素材。

## 缺口处理

缺口非空时，只有三种出路，必须与用户确认后择一：

1. **补素材** —— 用户提供文件，放进对应 `Assets/` 子目录，然后重新清点。
2. **改设计** —— 回到 `script-writer`，把依赖缺失素材的情节改掉（换素材或改剧情）。
3. **接受取舍** —— 用户明确同意「这个素材没有也可以」，记入设计稿的已知取舍，不再算缺口。

素材无法补齐、用户也不愿改设计时，**停在此阶段**并如实说明，不要带着缺口进入落盘。
