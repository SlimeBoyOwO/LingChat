<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 存储权限声明：Sherpa-ONNX 模型目录默认位于应用沙箱
         （Android/data/<package>/files/sherpa_onnx_models，免权限），
         以下权限用于支持把模型目录放到外部存储根（如 /sdcard/.lc-sherpa-onnx）的
         自定义场景，属备用声明，默认路径下不会触发。 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- “所有文件访问权限”：仅在模型目录位于外部存储根（Android 11+）时需要。
         无法用运行时对话框申请，只能经 Settings.ACTION_MANAGE_APP_ALL_FILES_ACCESS_PERMISSION
         引导授权（StoragePermissionPlugin.kt 提供命令）。
         tools:ignore="ScopedStorage" 关闭 lint 的 StrictMode 警告。 -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- 必须声明：WebView 的 onPermissionRequest 请求麦克风时捆绑请求
         MODIFY_AUDIO_SETTINGS + RECORD_AUDIO，未声明的权限被系统直接拒绝
         且不弹窗，导致整个请求被判失败（NotAllowedError: Permission denied）。
         normal 权限，声明即授予，无需运行时请求。 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 音频设备名称授权（getUserMedia 解锁 enumerateDevices 的真实设备名）已移除：
         音频输出设备切换仅桌面端可用，Android 上 audioOutputManager 直接跳过，无需麦克风权限。
         RECORD_AUDIO 仍保留：WebView 申请麦克风时系统需要该权限声明（ASR 录音）。 -->

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.ling_chat"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
    <!-- ANDROID FS PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
    
    <!-- ANDROID FS PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
</manifest>
